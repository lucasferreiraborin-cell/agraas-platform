import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Criar conta",
  description:
    "Cadastre-se na Agraas em 2 minutos. Rastreie seu rebanho, certifique para Halal e venda com dados verificados.",
};

export default function CadastroLayout({ children }: { children: React.ReactNode }) {
  return children;
}
