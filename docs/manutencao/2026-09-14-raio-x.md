# Raio-x de manutenção · 14/09/2026

> 8 lentes em paralelo (fiscal, auth/RLS, deps/build, banco/migrations, docs,
> público/compliance, testes, dívida de código), cada achado verificado por um
> cético independente com evidência de arquivo ou de produção.
> **79 achados · 78 confirmados · 1 refutado (mantido como hipótese).**
> Resultado bruto: `subagents/workflows/wf_f562dcd6-08f/journal.jsonl` (sessão).

## Semáforo — manhã → fim do dia

| Lente | Manhã | Agora | O que mudou |
|---|---|---|---|
| Fiscal | 🔴 | 🟡 | F1 CFOP (toda compra virava "erro"), F2, F3, F4, F6, F9, F10 fechados (`33a36fb`). Abertos: F5 (composição do vNF), F7 (apply-stock idempotência), F8 (RLS da tabela de alertas — precisa do banco) |
| Auth / RLS | 🔴 | 🟡 | AUTH-01, 03, 07, 09 fechados (`49f1196`, `a3bc3c0`). AUTH-02 e 05 escritos na **163 (não aplicada)**. Abertos: AUTH-04 (RPC de score revogado), 06 (digest sócios), 08 (rate limit em memória), 10 (trilha antes do reset) |
| Deps / build | 🟡 | 🟢 | next 16.3.5 (2 críticos + 12 altos), js-yaml@4, tsx. Abertos: eslint ausente (BD-04), pgTAP nunca roda (BD-06), higiene (BD-08) |
| Banco / migrations | 🟡 | 🟡 | DB-01 fechado. Abertos e **dependentes de acesso ao banco**: DB-02 baseline do esquema real, DB-03 RLS de `fiscal_notes_alerts_legacy`, DB-04 dedup da 160, DB-06 `search_path`, DB-10 seeds da FSJBE nas migrations |
| Docs | 🟡 | 🟡 | DOC-01, 04, 07, 09 fechados (CLAUDE.md/STATUS). Abertos: DOC-02 (regra deploy × migration), 05 (`platform_jobs_log` sem migration), 06 ("Foco atual" de maio), 08, 10 |
| Público | 🔴 | 🟡 | P1 (Jussara-GO confirmado), P2, P3, P4, P5, P8 fechados. Abertos: P6 (HALAL_ENABLED em 3 pontos), P7 (/planos com números fixos — rota pausada), P9 (OG herdado de grãos), P10 (soft-404 do passaporte) |
| Testes | 🟡 | 🟡 | +56 testes hoje (337 → 393). Abertos: T-02 (3 testes que testam o mock), T-03 (rota de upload sem teste de orquestração), T-04 (persona), T-05 (score engine), T-06 (FUNRURAL) |
| Dívida | 🟡 | 🟡 | DT-01 fechado. Abertos: DT-02 (**LCDPR simplificado soma todos os tenants para admin**), DT-03 (apply-stock sem checar erro), DT-05 (70 pontos de data UTC restantes; `lib/date-br.ts` criado), DT-06 (11 órfãos), DT-09 (`as any`) |

## O que entrou hoje (commits)

| Commit | Conteúdo |
|---|---|
| `8bcdbf0` | Ingestão unificada: XML/PDF/CSV/XLSX por um só `saveNote`; 4 endpoints da controladoria que não existiam; parse-doc reescrito |
| `33a36fb` | Lote 1 fiscal: CFOP de compra, dedup por chave (409), delete real, datas, botões visíveis, prompt sem norma inventada |
| `49f1196` | Lote 2 segurança: cron só por `CRON_SECRET`, e-mail sem relay, rotas públicas, next 16.3.5, modelos, copy pública |
| `a3bc3c0` | Lote 3 isolamento: `/admin/contas`, delete com diagnóstico, cadastro no servidor, migration 163 escrita |

## Só o Lucas pode (lista de 5 minutos)

1. **Vercel → Environment Variables → `CRON_SECRET`** (≥ 16 caracteres). Sem isso os 5 crons param com 401 (visível em `/admin/saude`). Antes, o header forjável os mantinha "funcionando".
2. **`/admin/contas`** logado como lucas@ → deixar **só a sua conta** como `admin`; FSJBE = `client`. Se aparecer e-mail duplicado, me diga qual.
3. **`/admin/reset-cliente`** → Bernardo · fsjdbe@gmail.com → Inventariar → e-mail → Apagar → "Sobrou depois: 0".
4. **`/admin/saude`** → conferir `ANTHROPIC_API_KEY` presente (PDF por IA depende) e `FISCAL_WRITE_MODE` (a controladoria lê a canônica; em `legacy` as notas aparecem só em `/fiscal`).
5. **Acesso ao banco** (`/mcp` → Supabase, ou uma janela sua no dashboard) para: DB-02 baseline, DB-03/F8 RLS da tabela de alertas, AUTH-02 (aplicar 163), 159–162.

## Regra 7 — contradições apontadas

- CLAUDE.md dizia "manter 5 animais fictícios na FSJBE" → decisão de 11/09 é o oposto; corrigido.
- CLAUDE.md "push em main = deploy automático" × "nunca deploy sem ordem": o que exige ordem é **migration e env da Vercel**; push é deploy e é aceito (DOC-02 — texto ainda a ajustar).
- Memória e docs diziam **Jandaia-GO**; o Lucas confirmou **Jussara-GO**. Coordenadas do seed (-17.048/-50.146) apontam para Jandaia — o seed cai no reset, a propriedade real entra com o endereço certo.
- Teste `cadastro-roles` lia a migration 162 como "CHECK vigente" enquanto produção roda a 126 — agora importa a fonte real e a rota tem fallback.
